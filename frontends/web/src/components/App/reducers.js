// import { ERROR } from '../../constants';

// export const errorReducer = (state = {}, { type, error }) => {
//   switch (type) {
//     case ERROR:
//       return error;
//     default:
//       return state;
//   }
// };
